specVersion: '2.2'
type: application
metadata:
  name: bookstore-ui5

resources:
  configuration:
    paths:
      webapp: webapp

server:
  customMiddleware:
    - name: ui5-middleware-simpleproxy
      afterMiddleware: compression
      configuration:
        baseUri: "http://localhost:8080"
        path: "/odata"
